<UserControl x:Class="ArmyBuilder.XMLEditor.Views.EditUnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArmyBuilder.XMLEditor.Views"
             mc:Ignorable="d" DataContext="{StaticResource MainViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ListView x:Name="UnitEntriesListView" ItemsSource="{Binding SelectedUnitEntry.Units}" SelectedItem="{Binding SelectedUnit}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button x:Name="AddButton" Content="Add"/>
            <Button x:Name="RemoveButton" Content="Remove" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Controls for editing a Unit -->
        <Grid Grid.Row="2" DataContext="{Binding SelectedUnit}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!--Id="1" Name="Archon" ForceOrg="0"-->

            <TextBlock Text="Id"/>
            <TextBlock Grid.Row="1" Text="Name"/>
            <TextBlock Grid.Row="2" Text="Minimum"/>
            <TextBlock Grid.Row="3" Text="Maximum"/>
            <TextBlock Grid.Row="4" Text="Base Cost"/>
            <TextBlock Grid.Row="5" Text="Cost per model"/>

            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Id}"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name}"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Minimum}"/>
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Maximum}"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding BaseCost}"/>
            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CostPerModel}"/>
        </Grid>
    </Grid>
</UserControl>
