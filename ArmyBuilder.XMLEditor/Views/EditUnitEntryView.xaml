<UserControl x:Class="ArmyBuilder.XMLEditor.Views.EditUnitEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArmyBuilder.XMLEditor.Views" 
             mc:Ignorable="d" DataContext="{StaticResource MainViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ListView x:Name="UnitEntriesListView" ItemsSource="{Binding SelectedArmy.UnitEntries}" SelectedItem="{Binding SelectedUnitEntry}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button x:Name="AddButton" Content="Add"/>
            <Button x:Name="RemoveButton" Content="Remove" Margin="10,0,0,0"/>
        </StackPanel>
        
        <!-- Controls for editing a UnitEntry -->

        <Grid Grid.Row="2" DataContext="{Binding SelectedUnitEntry}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

             <!--Id="1" Name="Archon" ForceOrg="0"-->
            
            <TextBlock Text="Id"/>
            <TextBlock Grid.Row="1" Text="Name"/>
            <TextBlock Grid.Row="2" Text="Force Org Id"/>
            <TextBlock Grid.Row="3" Text="Does not use force org"/>

            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Id}"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name}"/>
            <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Source={StaticResource ForceOrgCollection}}" SelectedIndex="{Binding ForceOrgSlot}"/>
            <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding DoeNotCountsTowardForceOrg}"/>
        </Grid>
    </Grid>
</UserControl>
