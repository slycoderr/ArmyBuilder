<Window x:Class="ArmyBuilder.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:views1="clr-namespace:ArmyBuilder.Windows.Views"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" WindowStartupLocation="CenterScreen"
        mc:Ignorable="d" DataContext="{StaticResource MainViewModel}" 
        Title="ArmyBuilder: List Builder" Height="800" Width="1200" Loaded="MainWindow_OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" MinHeight="150"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.RowSpan="4" Width="50" ZIndex="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <Button Content="Lists" Width="50" Height="50"/>
                <Button Content="List"  Width="50" Height="50"/>
            </StackPanel>

            <Grid Grid.Column="1" Width="300">
                <Grid x:Name="ArmyListsGrid" >
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ListView x:Name="ArmyListsListView" ItemsSource="{Binding Source={StaticResource ArmyListCollection}}" ItemTemplate="{StaticResource ArmyListTemplate}" SelectedItem="{Binding SelectedArmyList, Mode=TwoWay}" SelectionMode="Single" Margin="10" Grid.Column="1">
                        <ListView.GroupStyle>
                            <GroupStyle HidesIfEmpty="True" HeaderTemplate="{StaticResource ArmyListGroupTemplate}" />
                        </ListView.GroupStyle>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding EditSelectedListCommand}" />
                                <!--<ChangePropertyAction PropertyName="Visibility" TargetObject="{Binding ElementName=ArmyListsGrid}" Value="Collapsed" />
                                <core:ChangePropertyAction PropertyName="Visibility" TargetObject="{Binding ElementName=CurrentArmyListGrid}" Value="Visible" />-->
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ListView>
                    <views1:NewArmyListView Grid.Row="1" />
                </Grid >

                <Grid x:Name="CurrentArmyListGrid" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <views1:ForceOrgView Grid.Row="5" DataContext="{Binding CurrentArmyListViewModel, Source={StaticResource MainViewModel}}" x:Name="ForceOrgPanel" MinHeight="85" />


                    <TextBlock Grid.Row="1" Margin="10">
                            <Run FontSize="18" Text="{Binding CurrentArmyListViewModel.PointsRemaining}"/>
                    </TextBlock>
                    <TextBlock Grid.Row="2" Margin="10, 10, 0, 10">
                            <Run Text="points remaining of"/>
                            <Run Text="{Binding SelectedArmyList.PointsLimit}"/>
                    </TextBlock>
                </Grid>
            </Grid>
        </Grid>

        <StackPanel x:Name="HeaderPanel" Grid.Row="1" Grid.Column="1">
            <TextBlock FontSize="38"  Text="{Binding CurrentArmyList.Name, FallbackValue=ListName, Mode=TwoWay}" Margin="10"/>
            <StackPanel Orientation="Horizontal" >
                <TextBlock Text="{Binding PointsLeft, FallbackValue=500}" FontSize="18" Margin="10,0,0,10" Foreground="{StaticResource DimText}"/>
                <TextBlock Text="of"  FontSize="18" Margin="10,0,10,10" Foreground="{StaticResource DimText}"/>
                <TextBlock Text="{Binding CurrentArmyList.PointsLimit, FallbackValue=1200}"  FontSize="18" Margin="0,0,0,10" Foreground="{StaticResource DimText}"/>
                <TextBlock Text="Points Left"  FontSize="18" Margin="10,0,0,10" Foreground="{StaticResource DimText}"/>
            </StackPanel>
            <StackPanel  HorizontalAlignment="Left" Orientation="Horizontal" Margin="10,0,0,0">
                <Button Width="100" Height="25" Margin="0,0,10,0" Content="Remove Unit" Command="{Binding RemoveSelectedUnitCommand, BindsDirectlyToSource=True}"/>
            </StackPanel>
        </StackPanel>

        
        <ListView  x:Name="UnitListView" Grid.Row="2" ItemTemplate="{StaticResource ArmyListDataTemplate}" SelectedItem="{Binding SelectedUnit, Mode=TwoWay}" ItemsSource="{Binding Source={StaticResource ArmyListDataCollection}, Mode=OneWay}" SelectionMode="Single" Grid.RowSpan="2"
                  Grid.Column="1" Margin="10,0,0,0">
            <ListView.GroupStyle>
                <GroupStyle HidesIfEmpty="True" HeaderTemplate="{StaticResource ArmyListDataGroupTemplate}"/>
            </ListView.GroupStyle>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>

        </ListView>

        <views1:UnitEditor x:Name="UnitEditor" Grid.Column="2" Grid.Row="2" DataContext="{Binding SelectedUnit, Mode=OneWay}" Visibility="Visible" Margin="5,10,10,0" />
        <Menu Grid.Row="0" Grid.Column="1">
            <MenuItem Header="Add Unit" >
                <MenuItem x:Name="HqItemsMenu" Header="HQ" ItemsSource="{Binding HqUnits}"/>
                <MenuItem x:Name="TroopItemsMenu" Header="Troop" ItemsSource="{Binding TroopUnits}"/>
                <MenuItem x:Name="EliteItemsMenu" Header="Elite" ItemsSource="{Binding EliteUnits}" />
                <MenuItem x:Name="FastAttackItemsMenu" Header="Fast Attack" ItemsSource="{Binding FastAttackUnits}" />
                <MenuItem x:Name="HeavySupportItemsMenu" Header="Heavy Support" ItemsSource="{Binding HeavySupportUnits}"/>
                <MenuItem x:Name="LordOfWarItemsMenu" Header="Lord of War" ItemsSource="{Binding LordOfWarUnits}"/>
            </MenuItem>
            <MenuItem Header="Export">
                <MenuItem Header="As .txt" Command="{Binding ExportToTextFileCommand}"/>
            </MenuItem>
            
            <MenuItem Header="Tools">
                <MenuItem x:Name="ManageArmyListButton" Header="Manage Army Lists" Command="{Binding CloseCommand}" />
            </MenuItem>
        </Menu>
        
        <!--<StackPanel x:Name="ProgressPanel" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" Grid.RowSpan="10" Grid.ColumnSpan="10">
            <controls:ProgressRing Width="50" Height="55" IsActive="True" />
            <TextBlock Text="Loading... Please Wait" />
        </StackPanel>-->
    </Grid>
</Window>
